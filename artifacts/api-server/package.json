{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "build": "echo 'python backend - no build step'",
    "start": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "typecheck": "echo 'python backend - no typecheck'"
  }
}
